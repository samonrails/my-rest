<% if @asset.new_record? %>
  alert("Failed to upload painting: <%= j @asset.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#add_new_image").append("<%= j render @asset %>");
  $("#progress_bar_container").remove();
<% end %>
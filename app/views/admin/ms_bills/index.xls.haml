.section-header
%h1
  MS - Bill

%h4 
  Generated At: #{Time.now.strftime("%Y-%m-%d %I:%M:%S %p")}
  %br
  Generated By:
  - unless @current_user.nil?
    #{@current_user.name} (#{@current_user.email})

%br

= render :partial => "admin/ms_bills/table", :formats => [:html], :locals => {allow_links: false}